{
  "buildCommand": "npm install --legacy-peer-deps && npm run build && find dist -type f -name '*.html' && ls -la dist/",
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "dist/front",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}